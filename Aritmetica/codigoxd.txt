    cont=0
    while(cont!=1):
        Limite=0
        for i in range(len(Maximos)):
            print(ValorDecoMin)
            print(Maximos)
            if(Maximos[i]<=ValorDecoMin):
                Limite=Maximos(i-1)
                cont=cont+1
            elif(Maximos[i]>=ValorDecoMin):
                Limite=Maximos[i]
                cont=cont+1

    cont2=0
    while(cont2!=1):
        Limite2=0
        for i in range(len(Minimos)):
            print(ValorDecoMin)
            print(Minimos)
            if(Minimos[i] <= ValorDecoMin):
                Limite2=Minimos[i]
                cont2=cont2+1
            elif(Minimos[i] >= ValorDecoMin):
                Limite2=Minimos[i-1]
                cont2=cont2+1

    print(Limite,"lim")
    print(Limite2,"lim min")

    DecoValores=list()
    for i in range(len(mensaje)): 
        print(Lim2,"min",Lim1,"max",ValorDecoMax,"val")    
        An=(ValorDecoMax-Lim2)/Lim1
        ValorDecoMax=An
        DecoValores.append(An)
    print(An)
    print(ValorDecoMin)
    print(DecoValores)